from setuptools import setup

setup(
    name='gemini',
    version='',
    packages=['posts', 'posts.migrations', 'theme', 'users', 'users.migrations', 'gemini'],
    url='',
    license='',
    author='dario',
    author_email='',
    description=''
)
